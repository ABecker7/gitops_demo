apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pod-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: pod
            port:
              name: pod
